{
  "name": "stream-management",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
   
    "start": "serve -s dist -l $PORT"
  },
 
  "devDependencies": {
    "vite": "^5.1.0"
  },
  "dependencies": {
    "serve": "^14.2.5"
  }
}
